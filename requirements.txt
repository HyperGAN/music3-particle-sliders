# CPU smoke (pytest, prompt cards, --dummy train/infer).
# The core pin pulls torch and particlegan. It does not download Hub weights.
PyYAML>=6
pytest>=8
particle-sliders-core @ git+https://github.com/HyperGAN/particle-sliders.git@4340e28bed388d50800c469525b460a108091da0#subdirectory=packages/particle-sliders-core

# A live CUDA run also needs the MiniMax Music 3 diffusers stack
# (diffusers, transformers, accelerate, safetensors) and a local checkout of
# MiniMaxAI/MiniMax-Music-3. Those are not required for --help or --dummy.
